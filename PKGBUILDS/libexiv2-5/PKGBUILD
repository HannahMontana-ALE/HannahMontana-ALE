# Generated by debtap
# Maintainer: Michael Monaco <thepoorpilot@gmail.com> 

pkgname=libexiv2-5
pkgver=0.18
pkgrel=1
pkgdesc="EXIF/IPTC metadata manipulation library. Control pkgbuildinstallations1 postinst postrm usr full read and write access to the EXIF and IPTC metadata of an image through Exiv2 keys and standard C++ iterators control pkgbuildinstallations1 postinst postrm usr a smart IPTC implementation that does not affect data that programs like Photoshop store in the same image segment o MakerNote tags can be accessed just like any other EXIF metadata control pkgbuildinstallations1 postinst postrm usr extract and delete methods for EXIF thumbnails (both, JPEG and TIFF thumbnails) control pkgbuildinstallations1 postinst postrm usr set methods for EXIF thumbnails (JPEG only, TIFF thumbnails can be set from individual tags) control pkgbuildinstallations1 postinst postrm usr complete API documentation (by Doxygen) control pkgbuildinstallations1 postinst postrm usr generic lower-level classes to access IFD (Image File Directory) data structures"
arch=('x86_64')
url="http://www.exiv2.org"
license=('custom')
groups=('')
depends=('expat>=1.95.8' 'gcc>=4.1.1' 'gcc-libs>=4.2.1' 'glibc>=2.4' 'zlib>=1.1.4')
optdepends=('exiv2')
options=('!strip' '!emptydirs')
install=${pkgname}.install

source_x86_64=("libexiv2-5_0.18-1ubuntu1_amd64.deb")

sha512sums_x86_64=('f1319267039a9f68702c78db854693c15c3fc7174d014d0fb59ac237cd09e727e9c783cdf3357e46cada14f22d1250e59c9ddbacdb879c78f5efcdc8e11c8bad')

package(){

	# Extract package data
	tar xzf data.tar.gz -C "${pkgdir}"

	install -D -m644 "${pkgdir}/usr/share/doc/libexiv2-5/copyright" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"

}
